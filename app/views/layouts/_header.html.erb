<header>
  <nav class="navbar navbar-expand-lg navbar-dark my-own">
    <a class="navbar-brand" href="/">
    <%= image_tag "logo.png", width: '30', height: '30', class: 'd-inline-block align-top', alt: 'logo' %>
    The Gossip Project</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item active">
          <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link" href="/gossips/new">Create a Gossip</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/gossips">Gossips List</a>
          </li>
        <% end %>
        <li class="nav-item">
          <a class="nav-link" href="/about">About us</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/contact">Contact</a>
        </li>
      </ul>
    </div>
    <% if user_signed_in? %>
      <p style="color: #fff; margin-top: 15px;">Logged in as 
      <% if current_user.username == nil %>
        <strong><%= current_user.email %></strong>.</p>
      <% else %>
        <strong><%= current_user.username %></strong>.</p>
      <% end %>
    <% end %>
  </nav>
</header>